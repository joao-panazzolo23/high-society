import 'package:flutter/cupertino.dart';
import 'package:high_society/features/store/models/store-item.model.dart';

class StoreItemDetails extends StatelessWidget {
  final StoreItemModel model;

  const StoreItemDetails({super.key, required this.model});

  //vai precisar disso aq dps
  // Column(
  // mainAxisSize: MainAxisSize.min,
  // )
  @override
  Widget build(BuildContext context) {
    // TODO: create modal to add model to the list
    // This class should have all widget information's
    // and also have an add button to show add it to the main page.
    //Main page will have an provider returning
    throw UnimplementedError();
  }
}
